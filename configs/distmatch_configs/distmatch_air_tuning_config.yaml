# Run candidates on the validation interval; preserve the final test boundary.
grid:
  model.match_threshold: [0.025, 0.05, 0.1]
  model.past_window_len: [25, 50, 100]

tuning:
  num_sequences: 2
  delta_threshold: -0.01

