# Starting settings shared with the existing NexCP presets.
data:
  data_path: "./data/solar_prediction/chronos/chronos_nsdb-60m_data.pkl"
  calibration_ratio: 0.66

model:
  rho: 0.99
  max_past: 100
  target_quantiles:
    - [0.05, 0.95]

plotting:
  plotting: true
  plotting_seq_len: 200

saving_dir: "./results/nexcp_chronos_solar/"
