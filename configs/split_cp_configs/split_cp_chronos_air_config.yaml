# Chronological calibration/test split of the saved held-out forecasts.
num_cores: 1

data:
  data_path: "./data/air-10_prediction/chronos/chronos_air-10_data.pkl"
  calibration_ratio: 0.66
  test_ratio: 0.34

model:
  target_quantiles:
    - [0.9, 0.1]
  prediction_step: 1

plotting:
  plotting: true
  plotting_seq_len: 200

saving_dir: "./results/split_cp/air/chronos/${now:%Y-%m-%d_%H-%M-%S_%f}/"
